# Core ML
torch>=2.1.0
transformers>=4.46.0
accelerate>=0.27.0
qwen-vl-utils>=0.0.8

# ASR
faster-whisper>=1.0.0

# Inference (primary backend)
vllm>=0.6.0

# Vision
Pillow>=10.0.0
opencv-python>=4.8.0
numpy>=1.24.0

# Validation / RAG
chromadb>=0.5.0
sentence-transformers>=3.0.0

# Discord bot
discord.py>=2.4.0
aiohttp>=3.9.0
aiofiles>=23.0.0

# Web API
fastapi>=0.111.0
uvicorn[standard]>=0.29.0
python-multipart>=0.0.9

# Config
PyYAML>=6.0
python-dotenv>=1.0.0

# Dev: hot-patching (code changes take effect without restarting / reloading models)
jurigged>=0.6.0


# pip install vllm transformers accelerate qwen-vl-utils faster-whisper opencv-python chromadb sentence-transformers discord.py aiohttp aiofiles python-dotenv